<%= form_with(model: apartment, local: true) do |form| %>
  <% if apartment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apartment.errors.count, "error") %> prohibited this apartment from being saved:</h2>

      <ul>
      <% apartment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :locationID %>
    <%= form.number_field :locationID, id: :apartment_locationID %>
  </div>

  <div class="field">
    <%= form.label :apartmentName %>
    <%= form.text_field :apartmentName, id: :apartment_apartmentName %>
  </div>

  <div class="field">
    <%= form.label :numberOfRooms %>
    <%= form.number_field :numberOfRooms, id: :apartment_numberOfRooms %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
